import { AboutPage } from "@/app/PageComponents";

export const metadata = {
  title: "About Us — genCART",
  description: "Learn about the mission, values, and editorial process behind genCART.",
};

export default function Page() {
  return <AboutPage />;
}
